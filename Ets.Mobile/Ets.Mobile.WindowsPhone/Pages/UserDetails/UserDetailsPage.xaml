<Page
    x:Class="Ets.Mobile.Pages.UserDetails.UserDetailsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Ets.Mobile.Pages.UserDetails"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{StaticResource EtsBackgroundMainBrush}">

    <StackPanel DataContext="{Binding}" Margin="15,0,15,0">
        <StackPanel>
            <TextBlock Padding="5,0,0,0" Foreground="{StaticResource EtsTextSecondaryBrush}" FontSize="20">
                    <Run Text="{Binding FirstName}" />
                    <Run Text="{Binding LastName}" />
            </TextBlock>
            <TextBlock x:Uid="ProfileHeaderUid" Foreground="Black" Text="Profile" />
            <TextBlock Padding="5,0,0,0" Foreground="{StaticResource EtsTextSecondaryBrush}" Text="{Binding PermanentCode}" />
            <TextBlock Padding="5,5,0,0" Foreground="{StaticResource EtsTextSecondaryBrush}" FontSize="15" Text="{Binding Balance}" />
        </StackPanel>
        <Grid>
            <Border CornerRadius="100">
                <Image Source="{Binding ImageSource}" Width="150" Height="150" />
            </Border>
        </Grid>
    </StackPanel>
</Page>