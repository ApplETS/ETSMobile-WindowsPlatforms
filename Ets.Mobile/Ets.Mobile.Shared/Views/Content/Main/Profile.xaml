<UserControl
    x:Class="Ets.Mobile.Views.Content.Main.Profile"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Ets.Mobile.Views.Content.Main"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="800"
    d:DesignWidth="400">

    <StackPanel DataContext="{Binding}" Orientation="Horizontal">
        <Border CornerRadius="100">
            <Image Source="{Binding ImageSource}" Width="60" Height="60" />
        </Border>
        <StackPanel>
            <TextBlock Padding="5,0,0,0" Foreground="{StaticResource EtsTextSecondaryBrush}" FontSize="20">
                    <Run Text="{Binding FirstName}" />
                    <Run Text="{Binding LastName}" />
            </TextBlock>
            <TextBlock Padding="5,0,0,0" Foreground="{StaticResource EtsTextSecondaryBrush}" Text="{Binding PermanentCode}" />
            <TextBlock Padding="5,5,0,0" Foreground="{StaticResource EtsTextSecondaryBrush}" FontSize="15" Text="{Binding Balance}" />
        </StackPanel>
    </StackPanel>
</UserControl>
